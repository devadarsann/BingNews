<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kannan.devan.bingnews">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".ArticelsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.kannan.devan.bingnews.GlideConfig"
            android:value="GlideModule" />
        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".ArticelsActivity" />

        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_articels"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".NewsReadActivity" />
        <activity android:name=".NewsBrowser"></activity>
    </application>

</manifest>